/**
 * Bounties DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: bounties
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Bounties Domain Module - Composition root
 *
 * DDD: Composition root for bounties domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  BountyRepositoryAdapter,
  CloseRepositoryAdapter,
  EnrollmentRepositoryAdapter,
  FreezeRepositoryAdapter,
} from "@fedbounty/adapters/bounties";
import { getIdGeneratorService } from "@fedbounty/adapters";
import type { AdapterDynamoDBClient } from "@fedbounty/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCloseBounty,
  ExecuteCreateBounty,
  ExecuteEnrollWorker,
  ExecuteFreezeBounty,
  ExecuteGetBounty,
  ExecuteListBounties,
  ExecuteListBountyEnrollments,
  ExecutePublishBounty,
  ExecuteUpdateBounty,
} from "@fedbounty/services/bounties/usecases";
// Ports (interfaces)
import type {
  BountyRepository,
  CloseRepository,
  EnrollmentRepository,
  FreezeRepository,
} from "@fedbounty/services/bounties/ports";

export interface BountiesDomainModule {
  repos: {
    bounties: BountyRepository;
    closes: CloseRepository;
    enrollments: EnrollmentRepository;
    freezes: FreezeRepository;
  };
  useCases: {
    bounties: {
      create: ExecuteCreateBounty;
      get: ExecuteGetBounty;
      list: ExecuteListBounties;
      update: ExecuteUpdateBounty;
    };
    closes: {
      get: ExecuteCloseBounty;
    };
    enrollments: {
      create: ExecuteEnrollWorker;
      list: ExecuteListBountyEnrollments;
    };
    freezes: {
      get: ExecuteFreezeBounty;
    };
  };
}

export function buildBountiesDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): BountiesDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      bounties?: BountyRepository;
      closes?: CloseRepository;
      enrollments?: EnrollmentRepository;
      freezes?: FreezeRepository;
    } = {};

    obj.bounties = new BountyRepositoryAdapter(dynamoClient);
    obj.closes = new CloseRepositoryAdapter(dynamoClient);
    obj.enrollments = new EnrollmentRepositoryAdapter(dynamoClient);
    obj.freezes = new FreezeRepositoryAdapter(dynamoClient);
    return {
      bounties: obj.bounties!,
      closes: obj.closes!,
      enrollments: obj.enrollments!,
      freezes: obj.freezes!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    bounties: {
      create: new ExecuteCreateBounty(executionContext, idGenerator, repos.bounties),
      get: new ExecuteGetBounty(executionContext, idGenerator, repos.bounties),
      list: new ExecuteListBounties(executionContext, idGenerator, repos.bounties),
      update: new ExecuteUpdateBounty(executionContext, idGenerator, repos.bounties),
    },
    closes: {
      get: new ExecuteCloseBounty(executionContext, idGenerator, repos.closes),
    },
    enrollments: {
      create: new ExecuteEnrollWorker(executionContext, idGenerator, repos.enrollments),
      list: new ExecuteListBountyEnrollments(executionContext, idGenerator, repos.enrollments),
    },
    freezes: {
      get: new ExecuteFreezeBounty(executionContext, idGenerator, repos.freezes),
    },
  };
  return { repos, useCases: useCases as BountiesDomainModule["useCases"] };
}
