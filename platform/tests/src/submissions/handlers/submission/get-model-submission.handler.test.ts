/**
 * GetModelSubmission handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: submissions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getModelSubmission } from "@fedbounty/api-server/domains/submissions/handlers";
import type { SubmissionsDDDDependencies } from "@fedbounty/api-server/domains/submissions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@fedbounty/tests/shared";
import { SUBMISSIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetModelSubmission", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SubmissionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SubmissionsDDDDependencies>(
      SUBMISSIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetModelSubmission, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { submissionId: entity.id };

    await getModelSubmission(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetModelSubmission.execute).toHaveBeenCalledWith(expect.objectContaining({ submissionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { submissionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetModelSubmission, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getModelSubmission(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
