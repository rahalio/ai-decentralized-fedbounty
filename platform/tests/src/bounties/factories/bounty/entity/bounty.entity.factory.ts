/**
 * Bounty Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: bounties
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { BountyEntity } from "@fedbounty/core/bounties/models/index.js";
import { bountiesSchemas } from "@fedbounty/core/bounties";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "bty";

export function createBounty(overrides: Partial<BountyEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    baseModelCid: overrides.baseModelCid ?? faker.string.uuid(),
    bountyId: overrides.bountyId ?? `bty_${ulid().toLowerCase()}`,
    name: overrides.name ?? faker.person.fullName(),
    privacyMode: overrides.privacyMode ?? "federated",
    rewardCurve: overrides.rewardCurve ?? [],
    status: overrides.status ?? "draft",
    validationSnapshotId: overrides.validationSnapshotId ?? `snp_${ulid().toLowerCase()}`,
    ...overrides,
  };

  return bountiesSchemas.Bounty.parse(raw);
}

export function bountyBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createBounty(overrides));
}
