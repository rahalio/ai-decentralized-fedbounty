/**
 * UpdateBounty handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: bounties
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateBounty } from "@fedbounty/api-server/domains/bounties/handlers";
import type { BountiesDDDDependencies } from "@fedbounty/api-server/domains/bounties";
import { createBounty as createBountyEntity } from "@fedbounty/factories/bounties";
import { createUpdateBountyRequest } from "@fedbounty/factories/bounties";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@fedbounty/tests/shared";
import { BOUNTIES_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateBounty", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: BountiesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<BountiesDDDDependencies>(
      BOUNTIES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createBountyEntity({ orgId });
    const input = createUpdateBountyRequest();
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { bountyId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateBounty, "execute").mockResolvedValue(useCaseResult);

    await updateBounty(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateBounty.execute).toHaveBeenCalledWith(expect.objectContaining({ bountyId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
