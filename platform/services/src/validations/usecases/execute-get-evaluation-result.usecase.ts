/**
 * ExecuteGetEvaluationResult - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: validations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetEvaluationResult Use Case
 *
 * DDD: Application use case for get Validation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetEvaluationResultInput, GetEvaluationResultOutput } from "../dto/validation.dto";
import type { ExecutionContextService, IdGeneratorService } from "@fedbounty/services/_shared/index.js";
import type { ValidationRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetEvaluationResult {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly validation: ValidationRepository
  ) {}

  async execute(input: GetEvaluationResultInput): Promise<GetEvaluationResultOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.vldId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).evaluationId) {
      throw new ValidationError("evaluationId is required");
    }
    const evaluationId = (input as any).evaluationId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.validation.getEvaluationResult({ evaluationId: evaluationId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${evaluationId}`);
    }

    return entity as GetEvaluationResultOutput;
  }
}
